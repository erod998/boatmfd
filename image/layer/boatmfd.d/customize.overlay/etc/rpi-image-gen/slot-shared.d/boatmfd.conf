# What the boat writes (boat-dashboard.service: tracks, waypoints, settings, calibration), on the
# data partition: the same whichever system copy is running, and kept across updates.
Version=1
Path=/var/lib/boatmfd
